/*******************************
 ** CLASSMODEL SPECIFICATION
 *******************************
 ** Conversation
 **	-> Verify
 **	-> Verify
 **	-> Refuse
 **	-> Repeat
 *******************************/

	classDef:Void		( lexConversation,
						  lexVerify,
						  lexVerify,
						  lexRefuse,
						  lexRepeat,
						  lexRepeatFull )

	classDef:ElementRange	( Verify,
						  Verify,
						  Refuse,
						  Repeat:
							{ 'on' } )


	classDef:StructureComplex 		( Conversation:
							(Verify,
							 Verify,
							 Refuse,
							 Repeat) )

	classDef:Synonym			( ConversationList, ConversationPunct = Conversation )

	classDef:StructureComplex		( Denial:
							( Refuse ) )

	classDef:StructureComplex		( Assert:
							( Verify ) )

	ParserGrammaticalScore:FocalCats = lexVerify, lexVerify, lexRefuse, lexRepeat, Conversation
	UnifierGenusnticScore:FocalCats = Conversation
